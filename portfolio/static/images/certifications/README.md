Drop certification badge / company logo images here, then reference the
filename in the "image" field of a CERTIFICATIONS entry in app.py.

You can also skip this folder entirely and use a full http(s) URL instead —
both are supported, see the comment above CERTIFICATIONS in app.py.
